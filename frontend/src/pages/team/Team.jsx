import { useEffect, useState } from "react";
import api from "../../services/api";
export default function Team() { const [members, setMembers] = useState([]); useEffect(() => { api.get("/team").then(({ data }) => setMembers(data.data)).catch(() => {}); }, []); return <div className="min-h-[calc(100vh-4rem)] bg-[#0b1220] p-6 text-white"><h1 className="text-3xl font-semibold">Team</h1><p className="mt-2 text-slate-400">People collaborating in your DevFlow workspace.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{members.length ? members.map((member) => <article key={member._id} className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-full bg-blue-500 font-semibold">{member.name[0]}</div><div><h2 className="font-medium">{member.name}</h2><p className="text-sm text-slate-400">{member.email}</p></div></div><p className="mt-4 text-sm capitalize text-blue-300">{member.role}</p></article>) : <p className="text-slate-400">No team members found yet.</p>}</div></div>; }
