import { useEffect, useState } from "react";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import api from "../../services/api";
export default function Analytics() { const [data, setData] = useState(null); useEffect(() => { api.get("/analytics/dashboard").then(({ data: response }) => setData(response.data)).catch(() => setData({ taskOverview: {} })); }, []); const chart = Object.entries(data?.taskOverview || {}).map(([name, value]) => ({ name: name.replace("_", " "), value })); return <div className="min-h-[calc(100vh-4rem)] bg-[#0b1220] p-6 text-white"><h1 className="text-3xl font-semibold">Analytics</h1><p className="mt-2 text-slate-400">Live workspace performance and delivery trends.</p><section className="mt-6 rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="font-semibold">Task completion</h2><div className="mt-5 h-80"><ResponsiveContainer width="100%" height="100%"><BarChart data={chart}><CartesianGrid stroke="#1e293b" vertical={false} /><XAxis dataKey="name" stroke="#94a3b8" /><YAxis stroke="#94a3b8" allowDecimals={false} /><Tooltip contentStyle={{ background: "#0f172a", border: "1px solid #334155" }} /><Bar dataKey="value" fill="#60a5fa" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div></section></div>; }
