import { useEffect, useState } from "react";
import { Bell } from "lucide-react";
import api from "../../services/api";
export default function Notifications() { const [items, setItems] = useState([]); useEffect(() => { api.get("/notifications").then(({ data }) => setItems(data.data)).catch(() => {}); }, []); const markAll = async () => { await api.put("/notifications/read-all"); setItems((current) => current.map((item) => ({ ...item, read: true }))); }; return <div className="min-h-[calc(100vh-4rem)] bg-[#0b1220] p-6 text-white"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-semibold">Notifications</h1><p className="mt-2 text-slate-400">Updates that need your attention.</p></div><button onClick={markAll} className="rounded-lg border border-slate-700 px-3 py-2 text-sm text-blue-300">Mark all as read</button></div><div className="mt-6 space-y-3">{items.length ? items.map((item) => <article key={item._id} className={`flex gap-3 rounded-xl border p-4 ${item.read ? "border-slate-800 bg-slate-900" : "border-blue-500/30 bg-blue-500/5"}`}><Bell className="mt-0.5 text-blue-300" size={18} /><div><h2 className="font-medium">{item.title}</h2><p className="mt-1 text-sm text-slate-400">{item.message}</p></div></article>) : <p className="rounded-xl border border-slate-800 bg-slate-900 p-6 text-slate-400">You’re all caught up.</p>}</div></div>; }
