import { motion } from "framer-motion";
export default function SplashScreen({ onComplete }) { return <motion.div initial={{ opacity: 1 }} exit={{ opacity: 0 }} transition={{ duration: 0.35 }} className="fixed inset-0 z-[100] grid place-items-center bg-[#0b1220] text-white"><div className="text-center"><motion.div initial={{ scale: 0.6, rotate: -18 }} animate={{ scale: 1, rotate: 0 }} transition={{ type: "spring", stiffness: 180 }} className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-blue-500 text-3xl font-bold shadow-xl shadow-blue-500/30">D</motion.div><motion.h1 initial={{ y: 12, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ delay: 0.2 }} className="mt-5 text-3xl font-semibold">DevFlow</motion.h1><p className="mt-1 text-sm text-slate-400">Build. Collaborate. Deliver.</p><motion.div className="mx-auto mt-6 h-1 w-48 overflow-hidden rounded-full bg-slate-800"><motion.div className="h-full bg-blue-400" initial={{ width: "0%" }} animate={{ width: "100%" }} transition={{ duration: 0.85 }} onAnimationComplete={onComplete} /></motion.div></div></motion.div>; }
