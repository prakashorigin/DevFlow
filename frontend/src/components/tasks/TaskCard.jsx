import { GripVertical, Trash2 } from "lucide-react";
import { statusLabel } from "../../utils/helpers";

const priorityStyles = { low: "text-slate-300", medium: "text-blue-300", high: "text-orange-300", critical: "text-rose-300" };
export default function TaskCard({ task, onStatusChange, onDelete }) { return <article className="rounded-xl border border-slate-800 bg-slate-900 p-3 shadow-sm"><div className="flex gap-2"><GripVertical size={17} className="mt-0.5 shrink-0 text-slate-600" /><div className="min-w-0 flex-1"><h3 className="font-medium text-white">{task.title}</h3>{task.description && <p className="mt-1 line-clamp-2 text-xs leading-5 text-slate-400">{task.description}</p>}</div><button aria-label={`Delete ${task.title}`} onClick={() => onDelete(task._id)} className="h-fit rounded p-1 text-slate-600 hover:bg-rose-500/10 hover:text-rose-300"><Trash2 size={15} /></button></div><div className="mt-3 flex items-center justify-between gap-2"><span className={`text-xs font-medium capitalize ${priorityStyles[task.priority] || priorityStyles.medium}`}>{task.priority || "medium"}</span><select aria-label={`Status for ${task.title}`} value={task.status} onChange={(event) => onStatusChange(task._id, event.target.value)} className="rounded border border-slate-700 bg-slate-950 px-1.5 py-1 text-xs text-slate-300">{["todo", "in_progress", "in_review", "completed"].map((status) => <option key={status} value={status}>{statusLabel(status)}</option>)}</select></div></article>; }
