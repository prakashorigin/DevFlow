import { Activity } from "lucide-react";
export default function ActivityFeed({ activities = [] }) { return <section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold text-white">Recent activity</h2><div className="mt-4 space-y-4">{activities.length ? activities.map((item) => <div key={item._id || item.createdAt} className="flex gap-3 text-sm"><Activity size={16} className="mt-0.5 text-blue-300" /><div><p className="text-slate-300">{item.description}</p><p className="mt-1 text-xs text-slate-500">{item.createdAt ? new Date(item.createdAt).toLocaleString() : "Just now"}</p></div></div>) : <p className="text-sm text-slate-400">Activity will appear as your team works.</p>}</div></section>; }
